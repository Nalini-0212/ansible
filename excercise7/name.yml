---
- name: understanding users
  hosts: all
  become: yes
  vars_files:
    - /home/ubuntu/vprofile/excercise5/group_vars/vars.yml

  tasks:
    - name: create users
      ansible.builtin.user:
        name: "{{item}}"
        state: present
      loop: "{{users}}"
